# `4bb5acb` → `5c72380` 추가 변경 검토 — 2026-09-23

사용자가 GitHub compare API에서 확인한 변경 목록 10개와 업로드한 `source-delta-4bb5acb-to-5c72380.zip` 안의 최신 파일을 `4bb5acb` 선별 소스와 대조했습니다. 이 검토는 전체 바이너리 ZIP 검증과 별도입니다.

| 영역 | 최신 원본 변경 | 한국어 패치 처리 |
|---|---|---|
| `src/overrides/org/lwjglx/opengl/Display.java` | Windows 테두리 없는 창을 장식 없이 생성하고 창 위치 재배치 검사. Win32/X11의 프레임버퍼 스케일 처리 조정 | Java 코드는 원본 설치본을 사용; README에 변경 목적만 설명 |
| `src/pzopt/pzopt/FrameCap.java` | ESC 일시정지 메뉴가 열려 있으면 메뉴 FPS 제한을 적용 | 기존 번역명 유지. 메뉴 제한 동작 설명 추가 |
| `docs/override-edits.md`, `docs/windows-test.md` | 보더리스 창 이슈와 테스트 기록 추가 | 문서에 변경 내용과 검증 한계 반영 |
| `docs/workshop/workshop.txt`, `scripts/workshop.sh` | Workshop 제목 및 설명 조정 | 게임 번역 키 변경 불필요 |
| `CLAUDE.md`, 하네스 3개 | 개발 메모와 검증 도구 추가 | 게임용 번역 대상 아님 |

GitHub 비교 목록에는 번역용 네 UI Lua와 `README.md`가 포함되지 않았습니다. 따라서 기존 RC1 번역 키 571개를 유지했습니다. 이후 공식 `5c72380` 릴리스 ZIP의 빌드 정보를 확인했고, 게임에 설치된 원본 UI Lua 네 파일의 해시가 RC2의 예상값과 일치함을 확인한 뒤 한국어 패치를 적용했습니다.

---

# 원제작자 소스 비교 기록 — 2026-09-23

이 문서는 사용자가 내려받아 제공한 `win-b0bbce05d5-4bb5acb` 태그와 `c56627c5c497a00e058583b6fcca9f92b340a695` 커밋의 **선별된 소스·텍스트 파일**을 비교한 결과입니다. 비교 대상 ZIP에는 릴리스와 master 각각 428개, 총 856개의 파일이 있습니다. `source-inventory.csv`는 두 원본 ZIP의 전체 1,128개 항목을 기록하지만, 크기가 크거나 제외 확장자인 바이너리 파일의 **내용까지 비교한 것은 아닙니다**.

## 릴리스와 master의 차이

두 소스 ZIP의 428개 상대 경로는 서로 동일하며 내용이 다른 파일은 다음 세 개입니다.

| 파일 | 차이 |
|---|---|
| `docs/windows-test.md` | `4bb5acb` 릴리스의 G1 GC 기본값·저사양 텍스처 압축과 패키지 파일 수 기록 추가 |
| `docs/workshop/workshop.txt` | Workshop 검색용 제목과 릴리스 정보·파일 수 갱신 |
| `scripts/workshop.sh` | 위 Workshop 제목과 동일한 문자열 갱신 |

검사한 릴리스와 master의 `src/lua/client/pzopt/` UI 소스 네 개는 서로 바이트 단위로 같습니다. **따라서 당시 RC1의 코드 기준은 릴리스 `4bb5acb`였습니다.** master에 있는 Workshop 제목 변경을 게임 내부 패치에 임의로 반영하지 않았습니다.

## 기존 한국어 v4 (`be1f28a`) 대비 원본 변경

| 원본 파일 | 변경사항과 처리 |
|---|---|
| `pzopt_optimizations_options.lua` | 저사양 프로필에 `texcompress=true` 추가, 기본 GC의 G1 변경을 반영한 프로필 설명, 불리언 디스플레이 옵션 선택 처리, 지연 생성 중 `doLayout()` 조기 반환을 추가. 모든 실행 로직은 최신 원본을 보존하고 프로필 설명 두 개의 한국어 및 영어 폴백을 수정함. |
| `pzopt_optimizations_classes.lua` | `gcG1Cores`, `gcMode`, `gcPauseMs`가 `pzopt.GcChoice`와 연결됨. 해당 소스는 이 번역 패키지의 덮어쓰기 대상이 아니며 공식 원본 설치 패키지에서 제공되어야 함. |
| 나머지 UI Lua 3개 | 구버전 원본 대비 변경 없음. 검증된 v4 번역 훅을 재사용함. |

### ESC 메뉴 오류와의 관계

원본 최신 소스는 `MainOptions.doLayout()`이 `pzoptCreatePending` 상태에서 바닐라 레이아웃을 호출하지 않도록 방어합니다. 이전에 보고한 `MainOptions.lua:3922`의 `self.keyButtonWidth` 미초기화와 직접 맞닿은 수정입니다. 다만 **코드에서 수정된 경로를 확인했다는 뜻이며**, 사용자의 실제 멀티플레이 환경에서 새 릴리스가 정상 동작한다는 검증은 별도로 필요합니다.

### 알려진 번역 범위

번역 키 571개씩(한국어·영어)은 기존 v4와 동일하며, 이번 변경으로 업데이트된 키는 저사양 프리셋 설명 두 개입니다. 새 GC 옵션 클래스 연결은 번역된 이름의 새 항목이 추가됐다는 뜻은 아닙니다. F9 Java 오버레이와 이미지에 삽입된 텍스트는 이 패키지 범위 밖입니다.

## 검증과 남은 작업

- 정적 검증: 네 UI Lua의 Lua 5.4 문법 파싱, ASCII 소스 여부, 한·영 JSON 키 집합 동일성, 정적 번역 키 참조, 최신 원본 `doLayout` 및 프리셋 코드 보존 확인.
- 로컬 추가 검증(2026-09-23): 기존 `be1f28a` 원본 421개 검증·백업, 공식 `5c72380` 원본 422개 설치·검증, RC2 설치 성공. 사용자가 제공한 게임 화면에서 한국어 설정 항목과 설명 표시를 확인했고, 해당 ESC 테스트에서 오류가 보고되지 않음.
- 아직 검증하지 않은 것: 전체 설정의 표시·저장 동작, ESC 장시간 반복 테스트, 운영 서버에서의 멀티 접속. `4bb5acb` 정식 설치는 별도로 수행하지 않음.

원본 README의 저사양 프리셋 설명 중 일부는 구형 G1 런처 JSON을 별도로 사용하도록 안내하지만, 실제 `4bb5acb` UI 소스는 G1이 `gcMode` 기본값이라고 설명합니다. 배포 문서에서는 **최신 실행 소스의 동작을 기준**으로 기술하며, 설명 불일치는 원제작자에게 확인할 수 있습니다.
